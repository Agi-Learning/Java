package com.luv2code.springdemo;

public interface Coach {
	public String getDailyWorkout();
	// Interface, Let's go ahead and add this one method, getDailyWorkout, 
	// Interface doesn't have any implementation cod, its only a specification. simply say what is available but not how its implemented.
}
